import React from 'react';
import {Icon} from 'antd';

const Loading = props => {
    return (
        <div className="loading">
            <Icon type='loading'/>
        </div>
    );
};

export default Loading;